[package]
name = "easy-fs-fuse"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.3.5", features = ["derive"] }
easy-fs = { path = "../os/easy-fs" }
env_logger = "0.10.0"
block-dev = { path = "../os/block-dev" }
